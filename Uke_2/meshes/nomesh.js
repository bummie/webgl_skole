function NoMesh()
{
    this.position = [ 0, 0, 0 ];
    this.rotation = [ 0, 0, 0 ];
    this.scale = [ 1, 1, 1 ];
    
    /**
     * Draws the mesh to the canvas
     * @param {*} gl 
     * @param {*} programInfo 
     */
    this.draw = function(parent, gl, programInfo)
    {   
        
    }
}